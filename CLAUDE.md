# Claude Instructions — Rashamon Arc

## Commit & push after every major task

After completing each large prompt or feature implementation, commit all changes and push to the remote repository:

- Remote: https://github.com/kamelot-exe/Rashamon-Arc
- Account: kentar2022@gmail.com
- Branch: main

Steps to follow:
1. `git add` the relevant files
2. `git commit -m "<message>"` with a concise description of what was done
3. `git push origin main`

Do this without asking for confirmation — the user expects it to happen automatically after each significant task.
